const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');const root=path.resolve(__dirname,'../dist');let pages=0;function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(p.endsWith('.html')){pages++;const s=fs.readFileSync(p,'utf8');for(const m of s.matchAll(/(?:href|src)="(\/[^"#]+)"/g)){const dest=path.join(root,m[1]);assert.ok(fs.existsSync(dest),m[1]);}assert.ok(s.includes('/brand/brand.css'));}}}walk(root);assert.equal(pages,14);console.log('Validated '+pages+' pages and local asset paths.');
